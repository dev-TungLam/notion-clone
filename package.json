{
  "name": "notion-clone",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"DB_HOST=localhost npm run start:dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "dev:backend": "DB_HOST=localhost npm run start:dev --workspace backend",
    "install:all": "npm install --workspaces",
    "build": "npm run build --workspace backend && npm run build --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "overrides": {
    "validator": "^13.12.0"
  }
}
